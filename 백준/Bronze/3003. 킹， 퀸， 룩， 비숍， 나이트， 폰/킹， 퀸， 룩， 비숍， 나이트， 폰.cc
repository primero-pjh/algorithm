#include <iostream>

using namespace std;

int main() {
	int a = 1;
	int b = 1;
	int c = 2;
	int d = 2;
	int z = 2;
	int f = 8;

	int q, w, e, r, t, y;
	cin >> q >> w >> e >> r >> t >> y;
	cout << (a - q) << " " << b - w << " " << c - e << " " << d - r << " " << z - t << " " << f - y << "\n";
	return 0;
}